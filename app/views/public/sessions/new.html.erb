<div class="container px-5 px-sm-0">
    <div class="d-flex row">
        <div class="col-6 mt-5 mx-auto">
            <h2 class="mb-5 text-center">ログイン</h2>

            <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
            <table class="table table-borderless table-inverse">
              <tbody>
                <tr>
                  <td>メールアドレス</td>
                  <td><%= f.email_field :email, autofocus: true, autocomplete: "email", class: 'form-control' %></td>
                </tr>
              　<tr>
                  <td>パスワード</td>
                  <td><%= f.password_field :password, autocomplete: "current-password", class: 'form-control' %></td>
                </tr>
            　   <tr>
                  <td>
                    <% if devise_mapping.rememberable? %>
                      <div class="field text-align-center">
                        <%= f.check_box :remember_me %>
                        <%= f.label :remember_me %>
                      </div>
                    <% end %>
                  </td>
                  <td><%= f.submit "ログイン", class: 'btn btn-success btn', style: 'width:100px' %></td>
                </tr>
              </tbody>
            </table>
            <% end %>
            <p class="mt-3 text-center">会員登録がお済みでない方は<%= render "public/shared/links" %>から新規登録を行ってください。</p>
        </div>
    </div>
</div>